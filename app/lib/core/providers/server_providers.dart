import 'package:flutter_riverpod/flutter_riverpod.dart';

// Bundled server providers removed in v2 — server is external (Bun or CF Workers).
// Keeping this file as a stub for any remaining imports.

/// Stub: initialize bundled server (no-op in v2)
Future<void> initializeBundledServer(ProviderContainer container) async {
  // No bundled server in v2
}
